def extract_landmarks(image):
    # Placeholder function for landmark extraction
    # Replace this with your actual landmark extraction code
    # For demonstration purposes, this function returns random landmarks
    import random
    landmarks = [(random.randint(0, image.shape[1]), random.randint(0, image.shape[0])) for _ in range(5)]
    return landmarks
